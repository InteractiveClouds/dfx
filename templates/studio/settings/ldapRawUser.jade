h4(style="margin-top: 10px;") Users to Import
table.userTable.table.table-condensed.table-thick-bordered
    thead(align="left")
        tr
            th(width="1em")
            th(width="1em") Login
            th Roles
            th
    tbody
        each user, i in list
            tr(class=(user._available ? '' : 'unavailable'), data-number="#{i}")
                td
                    label.x-large(style="padding:0 0.3em;")
                        input(type="checkbox", data-number="#{i}", checked=(user._available ? true : undefined), disabled=(user._available ? undefined : true))
                td(style="padding:0 1em;")=user.login
                td(data-name="roles")
                td
button.btn.btn-info#importButton
    span.fa.fa-lg.fa-cloud-download
    span(style="padding-left:5px") Import
